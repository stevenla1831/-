# ─── LINE LIFF ────────────────────────────────────────────────────
# 取自 LINE Developers Console → LIFF → LIFF ID
# From LINE Developers Console → LIFF → LIFF ID
VITE_LIFF_ID=2009693707-DihGx8m5

# ─── Firebase ─────────────────────────────────────────────────────
# Firebase config is embedded in firebase-applet-config.json
# No additional env vars required for Firebase

# ─── 部署 ─────────────────────────────────────────────────────────
# APP_URL: The URL where this app is hosted (for OAuth callbacks etc.)
APP_URL=https://your-app.run.app

# ─── GAS 後端（選用）────────────────────────────────────────────────
# Google Apps Script Web App URL (set in GAS Script Properties instead)
# VITE_GAS_URL=https://script.google.com/macros/s/YOUR_ID/exec
